[project]
name = "ai-recruitment-agentcore-runtime"
version = "0.1.0"
description = "Minimal AgentCore Runtime for CV analysis"
requires-python = ">=3.10"

dependencies = [
    "bedrock-agentcore",
    "boto3",
    "botocore",
]

[tool.uv]
